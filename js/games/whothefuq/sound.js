/* sound.js — เสียงประกอบของ Who the fuq are you
   ─────────────────────────────────────────────────────────────
   ยังเป็นโครงเปล่า ไม่มีไฟล์เสียง — วางแบบเดียวกับเกมอื่น (ผูกกับสถานะ
   ไม่ใช่การกดปุ่ม) ไว้ก่อน พอเลือกไฟล์แล้วค่อยเติมลง FILES และ play() */

export const FILES = [];
export const preload = () => {};
export function reset() {}
export function play(/* st */) {}
